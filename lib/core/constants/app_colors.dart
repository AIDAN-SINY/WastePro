import 'package:flutter/material.dart';

/// App Colors
///
/// Centralized color palette for the application

class AppColors {
  // Primary Colors
  static const Color primary = Color(
    0xFF00A86B,
  ); // Green (Waste/Sustainability)
  static const Color primaryLight = Color(0xFF66C2A5);
  static const Color primaryDark = Color(0xFF00654B);

  // Secondary Colors
  static const Color secondary = Color(0xFF0066CC); // Blue
  static const Color secondaryLight = Color(0xFF3399FF);
  static const Color secondaryDark = Color(0xFF004499);

  // Accent Colors
  static const Color accent = Color(0xFFFFB700); // Gold
  static const Color accentLight = Color(0xFFFFD700);
  static const Color accentDark = Color(0xFFCC8800);

  // Status Colors
  static const Color success = Color(0xFF28A745);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFDC3545);
  static const Color info = Color(0xFF17A2B8);

  // Neutral Colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF6C757D);
  static const Color greyLight = Color(0xFFF8F9FA);
  static const Color greyDark = Color(0xFF343A40);

  // Background
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);

  // Borders
  static const Color border = Color(0xFFE0E0E0);
}
